# ═══════════════════════════════════════════════════════════════
#  .gitignore — Colonial Pipeline Case Study
#  Cybersecurity Research Project
# ═══════════════════════════════════════════════════════════════

# ─── Operating System Files ───────────────────────────────────

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*


# ─── Editor & IDE Files ───────────────────────────────────────

# VS Code
.vscode/
!.vscode/settings.json
!.vscode/extensions.json

# JetBrains (WebStorm, IntelliJ, PyCharm, etc.)
.idea/
*.iml
*.iws
*.ipr
out/

# Sublime Text
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache
*.sublime-workspace
*.sublime-project

# Vim / Neovim
*.swp
*.swo
*~
Session.vim
.netrwhist

# Emacs
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp


# ─── Node.js / npm (if any tooling is added) ──────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
.npm
.yarn/


# ─── Python (if any analysis scripts are added) ───────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python
env/
venv/
.venv/
ENV/
pip-log.txt
pip-delete-this-directory.txt
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/


# ─── Report Build / Export Artifacts ─────────────────────────

# PDF exports of the report (generate fresh, don't track binaries)
*.pdf

# Screenshot / preview images (auto-generated)
screenshots/
previews/
thumbs/

# Compressed archives of the report
*.zip
*.tar.gz
*.rar
*.7z


# ─── Sensitive & Security-Critical Files ──────────────────────

# Environment variables — NEVER commit secrets
.env
.env.*
.env.local
.env.development
.env.production
!.env.example

# API keys, tokens, credentials
*.key
*.pem
*.p12
*.pfx
*.crt
*.cer
secrets.json
credentials.json
config.secret.*
auth.json

# Private notes or raw research data not intended for public release
private/
confidential/
raw-data/
notes-private.*

# Any offline copies of breach data, IOC lists, or malware samples
# (should NEVER be committed to a public repository)
iocs/
samples/
malware/
hashes.txt
leaked-creds*


# ─── Logs & Temporary Files ───────────────────────────────────
*.log
*.tmp
*.temp
*.bak
*.orig
*.cache
.cache/
tmp/
temp/


# ─── Browser / Local Testing ──────────────────────────────────

# LiveServer / local dev
.live-server.json

# Browsersync
bs-config.js


# ─── Version Control Helpers ──────────────────────────────────
*.orig
*.rej
.merge_file_*


# ─── Documentation Build Output ──────────────────────────────
_site/
.jekyll-cache/
.jekyll-metadata
docs/_build/
site/


# ═══════════════════════════════════════════════════════════════
#  What IS intentionally tracked in this repository:
#
#   ✔  index.html       — Main interactive case study report
#   ✔  README.md        — Project overview & researcher profile
#   ✔  LICENSE          — MIT License + academic disclaimer
#   ✔  .gitignore       — This file
#
# ═══════════════════════════════════════════════════════════════
